env: "local"  # "local","development","production (prod)"
http-server:
  host: "localhost"
  port: 8080
  timeout: 4s   # time for reading request body, default is 4s
  idle-timeout: 60s  # time for idle connection, default is 60s

db-conn:
  host: "localhost"
  port: 5432
  user: shodruz
  database: myshop
  sslmode: disable
